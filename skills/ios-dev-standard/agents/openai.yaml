display_name: iOS 开发规范
short_description: 面向中文团队的原生 iOS 工程 skill，覆盖架构分层、状态归属、页面拆分、设计系统、自适应布局、数据层、并发、测试和交付验收。
default_prompt: 使用 ios-dev-standard 处理这个 iOS 任务。先分析现状和证据，再判断层级边界与状态归属；页面按职责拆分，自适应布局优先使用 Safe Area、Size Class、窗口尺寸和设计 token；在根因或边界明确后做最小必要修改，并验证原路径和邻近回归路径。
